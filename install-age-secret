#!/usr/bin/env bash

[ ! -f ./secrets/age-key ] && echo "./secrets/age-key does not exist. aborting installation" && exit 1

mkdir -p "$HOME"/.config/sops/age && cp ./secrets/age-key.txt "$HOME"/.config/sops/age/keys.txt
